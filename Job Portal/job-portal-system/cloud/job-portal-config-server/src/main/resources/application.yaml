server:
  port: 8888
spring:
  application:
    name: job-portal-config-server

  cloud:
    config:
      server:
        git:
          uri: "https://github.com/virendra-sirture05/job-portal-config"
          clone-on-start: true
          default-label : master
#          default-label : docker-config


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true