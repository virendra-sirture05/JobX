services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: jobx-backend

    ports:
      - "8080:8080"

    environment:
      SPRING_PROFILES_ACTIVE: local

    restart: unless-stopped